services:
  - type: web
    name: mindtrack
    env: python
    plan: free
    buildCommand: |
      cd frontend && npm install && npm run build && cd .. && pip install -r requirements.txt
    startCommand: gunicorn app_fixed:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
